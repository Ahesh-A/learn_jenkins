pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'github webhook triggered successfully'
                echo " main branch is triggered"
            }
        }
    }
}
